name: Tetris Tests


on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  tetris_tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: list some versions
      - run: | 
          ls -l
          docker --version
          docker images|grep bl3